# =============================================================================
# requirements.txt — Phishing Email Screenshot Detector
# Install with: pip install -r requirements.txt
# =============================================================================

# Web UI framework
streamlit==1.35.0

# Image processing
Pillow==10.3.0

# OCR wrapper (requires Tesseract binary installed separately — see README)
pytesseract==0.3.10

# Typing extensions (required by newer Streamlit on Python 3.8)
typing-extensions>=4.0.0
